# AIFriend 项目结构文档

## 一、项目概述

AIFriend 是一个 **AI 虚拟角色聊天平台**。用户可以创建自定义 AI 角色（名称、头像、性格、音色），与角色进行实时语音/文字对话，并上传文档构建知识库供角色检索回答。

核心 AI 能力基于 LangGraph 框架编排，使用 DeepSeek 等大语言模型进行对话生成，通过 WebSocket 与 TTS/ASR 服务交互。

---

## 二、技术栈

### 后端
| 技术 | 用途 |
|------|------|
| Django 6.0 + Django REST Framework | Web 框架和 REST API |
| SimpleJWT | JWT 认证（Access Token 2h + Refresh Token 7d） |
| LangGraph + LangChain | Agent 编排（对话图、记忆图） |
| LanceDB | 本地向量数据库（知识库 RAG 检索） |
| websockets | 与 TTS/ASR 云服务的 WebSocket 通信 |
| OpenAI SDK | 调用 LLM（DeepSeek）和 Embedding 服务 |

### 前端
| 技术 | 用途 |
|------|------|
| Vue 3 + Vite 8 | 前端框架和构建工具 |
| Pinia | 状态管理 |
| Vue Router 5 | 路由 |
| Axios + fetch-event-source | HTTP 请求 + SSE 流式请求 |
| TailwindCSS 4 + DaisyUI 5 | UI 框架 |
| @ricky0123/vad-web | 浏览器端语音活动检测（VAD） |
| MediaSource API | 流式音频边接收边播放 |

---

## 三、目录结构

```
AIFriend/
├── backend/                          # Django 后端
│   ├── manage.py
│   ├── requirements.txt
│   ├── db.sqlite3
│   ├── backend/                      # Django 项目配置
│   │   ├── settings.py               # 全局配置
│   │   └── urls.py                   # 根路由 + 媒体文件服务
│   ├── media/                        # 用户上传文件（头像、文档等）
│   └── web/                          # 主应用
│       ├── models/                   # 数据模型
│       │   ├── user.py               # UserProfile
│       │   ├── character.py          # Character, Voice
│       │   ├── friend.py             # Friend, Message, SystemPrompt
│       │   └── knowledge.py          # KnowledgeDocument, KnowledgeChunk
│       ├── views/                    # API 视图
│       │   ├── index.py              # SPA 入口（render index.html）
│       │   ├── user/account/         # 注册、登录、登出、Token刷新、用户信息
│       │   ├── user/profile/         # 个人资料编辑
│       │   ├── create/character/     # 角色 CRUD
│       │   │   └── voice/            # 音色列表 + 自定义音色管理
│       │   ├── homepage/             # 首页角色列表（搜索 + 分页）
│       │   ├── friend/               # 好友关系管理
│       │   ├── message/chat/         # AI 对话（核心）
│       │   │   ├── chat.py           # SSE 流式对话 + TTS 并行
│       │   │   └── graph.py          # ChatGraph Agent（tool calling）
│       │   ├── message/asr/          # 语音识别 WebSocket 中转
│       │   ├── message/memory/       # 长期记忆管理
│       │   │   ├── graph.py          # MemoryGraph
│       │   │   └── update.py         # 记忆更新逻辑
│       │   ├── knowledge/document/   # 知识库文档管理
│       │   │   ├── upload.py         # 上传 + 向量化入库
│       │   │   ├── list.py           # 文档列表
│       │   │   └── remove.py         # 删除文档 + 清理向量
│       │   └── utils/photo.py        # 头像覆盖保存工具
│       ├── documents/utils/           # RAG 工具
│       │   ├── custom_embeddings.py  # 自定义 Embedding（text-embedding-v4）
│       │   ├── document_loader.py    # 多格式文档加载器（PDF/DOCX/TXT/MD/CSV）
│       │   └── insert_documents.py   # 命令行批量入库脚本（已弃用）
│       ├── migrations/               # 数据库迁移
│       ├── templates/index.html      # Django 渲染的 SPA 入口（引用 Vite 构建产物）
│       └── urls.py                   # 主应用路由
│
├── frontend/                         # Vue 3 前端
│   ├── src/
│   │   ├── main.js                   # 入口（创建 App、挂载 Pinia/Router）
│   │   ├── App.vue                   # 根组件（拉取用户信息、认证守卫）
│   │   ├── router/index.js           # 路由表 + beforeEach 登录守卫
│   │   ├── stores/user.js            # Pinia 用户状态（登录/登出/Token管理）
│   │   ├── js/
│   │   │   ├── config/config.js      # 三环境配置（vue/django/cloud）
│   │   │   ├── http/api.js           # Axios 封装（自动 JWT 刷新 + 请求队列）
│   │   │   ├── http/streamApi.js     # SSE 流式请求封装（自动 Token 刷新重连）
│   │   │   └── utils/base64_to_file.js
│   │   ├── components/
│   │   │   ├── navbar/               # 导航栏、用户菜单、图标组件
│   │   │   └── character/            # 角色卡片、聊天窗口、输入框、麦克风
│   │   └── views/
│   │       ├── homepage/             # 首页角色广场
│   │       ├── friend/               # 好友列表
│   │       ├── create/               # 角色创建 + 编辑
│   │       ├── knowledge/            # 知识库管理
│   │       ├── user/account/         # 登录、注册
│   │       ├── user/profile/         # 个人资料
│   │       ├── user/space/           # 用户空间
│   │       └── error/                # 404
│   └── vite.config.js                # Vite 配置（构建输出到 ../backend/static/frontend）
│
└── 项目结构文档.md
```

---

## 四、数据模型

### UserProfile
| 字段 | 类型 | 说明 |
|------|------|------|
| user | OneToOne → Django User | 关联 Django 内置用户 |
| photo | ImageField | 头像，默认 default.png |
| profile | TextField(500) | 个人简介 |

### Character（AI 角色）
| 字段 | 类型 | 说明 |
|------|------|------|
| author | FK → UserProfile | 创建者 |
| name | CharField(50) | 角色名 |
| photo | ImageField | 角色头像 |
| voice | FK → Voice | 角色的音色 |
| profile | TextField | **核心字段**：角色性格设定，作为 system prompt 的一部分 |
| background_image | ImageField | 聊天背景图 |

### Voice（音色）
| 字段 | 类型 | 说明 |
|------|------|------|
| name | CharField(100) | 音色名 |
| voice_id | CharField(100) | 阿里云百炼平台的音色 ID |

### Friend（好友 = 用户×角色的聊天关系）
| 字段 | 类型 | 说明 |
|------|------|------|
| me | FK → UserProfile | 用户 |
| character | FK → Character | 角色 |
| memory | TextField(5000) | 长期记忆（LLM 自动提取和更新） |

### Message（聊天消息）
| 字段 | 类型 | 说明 |
|------|------|------|
| friend | FK → Friend | 所属聊天关系 |
| user_message | TextField(500) | 用户消息 |
| input | TextField(10000) | 完整模型输入（含 system prompt + 历史） |
| output | TextField(500) | AI 回复（截断存储） |
| input/output/total_tokens | IntegerField | Token 消耗统计 |

### SystemPrompt（系统提示词模板）
| 字段 | 类型 | 说明 |
|------|------|------|
| title | CharField(100) | 分类（"回复"、"记忆"） |
| order_number | IntegerField | 排序号 |
| prompt | TextField(10000) | 提示词内容 |

### KnowledgeDocument（知识库文档）
| 字段 | 类型 | 说明 |
|------|------|------|
| user | FK → UserProfile | 上传者 |
| filename | CharField(255) | 原始文件名 |
| file_type | CharField(20) | 文件类型（pdf/docx/txt/md/csv） |
| file | FileField | 文件实体 |
| file_size | IntegerField | 文件大小（字节） |
| chunk_count | IntegerField | 切分片段数 |
| status | CharField(20) | processing / ready / error |
| error_message | TextField | 错误信息 |

### KnowledgeChunk（文档片段）
| 字段 | 类型 | 说明 |
|------|------|------|
| document | FK → KnowledgeDocument | 所属文档 |
| chunk_index | IntegerField | 片段序号 |
| content | TextField | 片段内容 |

---

## 五、API 列表

| 方法 | 路径 | 功能 | 认证 |
|------|------|------|------|
| POST | `/api/user/account/register/` | 注册 | 否 |
| POST | `/api/user/account/login/` | 登录 | 否 |
| POST | `/api/user/account/logout/` | 登出 | 是 |
| POST | `/api/user/account/refresh_token/` | 刷新 Token | Cookie |
| GET | `/api/user/account/get_user_info/` | 获取用户信息 | 是 |
| POST | `/api/user/profile/update/` | 更新个人资料 | 是 |
| GET | `/api/homepage/index/` | 首页角色列表（搜索+分页） | 否 |
| POST | `/api/create/character/create/` | 创建角色 | 是 |
| POST | `/api/create/character/update/` | 更新角色 | 是 |
| POST | `/api/create/character/remove/` | 删除角色 | 是 |
| GET | `/api/create/character/get_single/` | 获取单个角色 | 是 |
| GET | `/api/create/character/get_list/` | 角色列表（按作者） | 是 |
| GET | `/api/create/character/voice/get_list/` | 音色列表 | 是 |
| POST | `/api/friend/get_or_create/` | 获取/创建好友关系 | 是 |
| POST | `/api/friend/remove/` | 删除好友 | 是 |
| GET | `/api/friend/get_list/` | 好友列表 | 是 |
| POST | `/api/friend/message/chat/` | AI 对话（SSE） | 是 |
| GET | `/api/friend/message/get_history/` | 聊天历史 | 是 |
| POST | `/api/friend/message/asr/asr/` | 语音识别 | 是 |
| POST | `/api/knowledge/document/upload/` | 上传文档 | 是 |
| GET | `/api/knowledge/document/list/` | 文档列表 | 是 |
| POST | `/api/knowledge/document/remove/` | 删除文档 | 是 |

---

## 六、前端路由

| 路径 | 页面 | 需登录 |
|------|------|--------|
| `/` | 首页角色广场 | 否 |
| `/friend/` | 好友列表 | 是 |
| `/create/` | 创建角色 | 是 |
| `/create/character/update/:id/` | 编辑角色 | 是 |
| `/knowledge/` | 知识库管理 | 是 |
| `/user/account/login/` | 登录 | 否 |
| `/user/account/register/` | 注册 | 否 |
| `/user/space/:user_id/` | 用户空间 | 否 |
| `/user/profile/` | 个人资料 | 是 |
| `/:pathMatch(.*)*` | 404 | 否 |

---

## 七、核心业务流程

### 7.1 AI 对话流程

```
用户发送消息
  → POST /api/friend/message/chat/ (SSE)
  → 构建 LangGraph 输入：
     1. SystemPrompt 表取 "回复" 类提示词
     2. 拼接角色性格（character.profile）
     3. 拼接长期记忆（friend.memory）
     4. 拼接知识库提示
     5. 拼接最近 10 条历史消息
     6. 追加当前用户消息
  → LangGraph Agent 调用 LLM（deepseek-v3.2，绑定 tool）
     - search_knowledge_base：搜索向量知识库
     - get_time：查询当前时间
  → LLM 决定是否调用 tool，调用则执行 tool → 再调 LLM 生成最终回复
  → 同时启动 TTS 并行任务：
     LLM 每个 text chunk → WebSocket 发给 TTS 服务 → 返回 audio
  → SSE 推送 text chunk 和 audio chunk 给前端
  → 前端：text 追加到聊天气泡，audio 通过 MediaSource 边下边播
  → 保存 Message，每 1 条消息触发记忆更新
```

### 7.2 知识库 RAG 流程

```
用户上传文档（pdf/docx/txt/md/csv）
  → KnowledgeDocument 记录入库
  → document_loader 解析文件内容（自动检测编码）
  → RecursiveCharacterTextSplitter 切分（500字/块，50字重叠）
  → CustomEmbeddings 向量化（text-embedding-v4, 1024维）
  → LanceDB 存储（append 模式，统一 metadata schema）
  → KnowledgeChunk 记录入库

对话时：
  → Agent 判断用户问题涉及文档 → 调用 search_knowledge_base tool
  → 相似度检索 top-3 最相关片段 → 注入 LLM 上下文生成回答
```

### 7.3 长期记忆更新

```
每 1 条消息后触发 MemoryGraph：
  → SystemPrompt 表取 "记忆" 类提示词
  → 输入：原始记忆 + 最近 10 条对话
  → LLM 生成压缩/整合后的新记忆
  → 更新 friend.memory
```

### 7.4 JWT 认证流程

```
登录 → 后端返回 Access Token（响应体）+ Refresh Token（httpOnly Cookie）
     → 前端 Pinia 存储 Access Token
     → Axios 拦截器自动附加 Bearer Token 到请求头
     → 401 时自动用 Refresh Token 刷新，并发请求排队等待
     → 刷新成功后重试所有排队请求
     → 刷新失败则登出
```

---

## 八、优化建议

### 高优先级

**1. ChatGraph 使用全局单例，避免每次对话重复创建**
- `graph.py:93` — `ChatGraph.create_app()` 在每次对话请求时都重新创建整个图（包括 LLM 实例和 tool 绑定），浪费资源。建议将 `create_app()` 改为模块级单例或使用 Django 缓存。

**2. MemoryGraph 缺少流式输出**
- `memory/graph.py:33` — 记忆更新用的是 `graph.invoke()`（非流式），每次更新都是一个完整的 LLM 请求。对于频繁触发的更新（每 1 条消息），累积 token 消耗较大。可考虑降低触发频率或使用更便宜的模型。

**3. 空消息检查后置**
- `chat.py:57` — `message.strip()` 的判空检查在 `create_app()` 之后，导致每次空消息请求仍然创建了整个 LangGraph 实例。应把判空提前到函数开头。

**4. 数据库 N+1 查询**
- `chat.py:34-35` — `SystemPrompt.objects.filter()` 每次对话都要查一次。可以加 Django 缓存（`@cached_property` 或 Redis）。

### 中优先级

**5. 硬编码的模型名散落各处**
- `graph.py:53`（`deepseek-v3.2`）、`memory/graph.py:15`（`deepseek-v3.2`）、`custom_embeddings.py:23`（`text-embedding-v4`）— 模型名硬编码在代码里。建议统一到 `settings.py` 中管理。

**6. 前端图标组件大量重复**
- `frontend/src/components/navbar/icons/` 和 `frontend/src/components/character/icons/` 中有多个重复的图标组件（`KeyboardIcon`、`MicIcon`、`RemoveIcon`、`SendIcon`、`UpdateIcon` 各出现两次）。应合并到一个公共 `icons/` 目录。

**7. 知识库不支持图片**
- `upload.py:16` — `ALLOWED_TYPES` 只有文本格式。如需支持图片上传（OCR 提取文字或多模态 embedding），需要扩展。

**8. 异常处理吞掉错误信息**
- `list.py:32`、`remove.py:48` — `except Exception: return Response('系统异常')` 吞掉了具体错误，排查问题困难。至少应打印日志（`logging.exception()`）。

**9. `insert_documents.py` 已过时**
- 使用相对路径和旧 API，功能已被 `upload.py` 取代。建议删除或在文件头标注"已弃用"。

### 低优先级

**10. CSS 文件分离**
- NavBar 和各个页面组件中 TailwindCSS 类大量重复。考虑提取为 `@apply` 指令或公共样式。

**11. TypeScript 使用不充分**
- `tsconfig.json` 已配置，但大部分前端代码仍是 `.js` 文件。建议新功能用 `.ts` 编写。

**12. 前端 config.js 环境切换**
- `config.js` 通过手动修改变量切换环境，容易误提交。建议改用 Vite 的 `.env` 文件 + `import.meta.env`。

**13. `friend/remove.py` 删除好友时未清理聊天记录**
- 删除 Friend 时，关联的 Message 会级联删除（Django 默认），但如需要保留数据则需要调整。

**14. 消息截断存储**
- `Message.output` 字段 max_length=500，`Message.user_message` 也是 500，长对话会被截断。可根据实际需求调整或用 TextField。
