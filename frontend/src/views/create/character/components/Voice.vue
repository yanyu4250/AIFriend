<script setup>
import {ref, watch} from "vue";

const props = defineProps(['voices','curVoiceId'])
const myVoice = ref(props.curVoiceId)

watch(() => props.curVoiceId, (newValue) => {
  myVoice.value = newValue
})

defineExpose({
  myVoice,
})
</script>

<template>
 <fieldset class="filedset">
    <label class="label text-base">音色</label>
    <select v-model="myVoice" class="select w-full">
      <option
          v-for="voice in voices"
          :id = 'voice.id'
          :value = 'voice.id'
      >{{ voice.name }}</option>
    </select>
 </fieldset>
</template>

<style scoped>

</style>